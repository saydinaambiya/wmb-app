import { Text, View } from "react-native";

const AddTable = () => {
    return (
        <View style={{
            flex: 1,
            justifyContent: "center"
        }}>
            <Text>Add Table</Text>
        </View>
    )
}

export default AddTable;